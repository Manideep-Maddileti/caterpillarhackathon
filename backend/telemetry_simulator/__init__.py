"""Realistic fleet telemetry simulator.

Standalone package (no dependency on the FastAPI app) that behaves like a
real IoT telematics feed: physics-based, state-persisting, event-driven,
POSTing to the backend's public REST API — see simulator.py for the
entrypoint.
"""
